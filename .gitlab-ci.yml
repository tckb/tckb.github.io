image: jojomi/hugo:latest

variables:
  GIT_SUBMODULE_STRATEGY: recursive

test:
  script:
  - hugo
  except:
  - master

pages:
  script:
  - HUGO_ENV=production hugo --gc --minify
  artifacts:
    paths:
    - public
  only:
  - master
